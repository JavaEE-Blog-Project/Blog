spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: admin
    password: 123456
    url: jdbc:h2:file:~/.myblog/db

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: true

  thymeleaf:
    cache: true